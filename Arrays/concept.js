
//in JS, array can have elements of same datatype or different data type in one array not like C. Array can have one more array as element.

// const array1 = [1,2]
//you can do array1[6], at 6the element will be there in between undefined elements.

//a.comments

// Shift + Alt + A for block Comment
// Ctrl + / for line comment

// Ctrl + `(backtick) for terminal